# 更新记录

## [v2.0.1] - 2026-09-17

### 修复

- `/kg b` 创建的路由现在直接持久化到 AstrBot 的 `notification_routes`，刷新 Dashboard 配置页即可查看和编辑。
- 插件启动时自动把 v2.0.0 保存在状态文件中的命令路由迁移到配置项；保存失败时保留原数据继续使用。
- `/kg ub` 同步删除配置中由命令创建的匹配路由，同时保护用户手动添加的路由。
- 回归套件扩展为 50 项，并使用真实 `AstrBotConfig` 验证配置文件落盘格式。

## [v2.0.0] - 2026-09-17

### 新插件身份

- 重命名为 `astrbot_plugin_komari_guard`、`KomariGuardPlugin` 和 Komari Guard。
- 命令统一收敛为 `/kg` 短指令组，不保留会与上游冲突的 `/komari_*` 命令。
- 常用查询提供 `/kg s`、`rt`、`his`、`ls`、`i`、`v`、`a`、`ck` 等短指令。
- 独立数据目录、配置、仓库和原创 Logo，可与上游插件共存。

### 新功能

- 新增通知路由：每条路由可独立指定目标 UMO、精确节点、告警开关与每日日报时刻。
- 新增 `/kg b [节点] [HH:MM] [alert|daily|both]`，快速将当前私聊或群聊绑定为全部/单节点通知目标。
- 新增 `/kg r` 路由查看、按路由独立日报进度、重叠路由去重。
- 新增持久告警待发队列；平台发送失败或静默期间不丢失告警。
- 节点状态改为在线/离线/未知三态，遥测数据源故障不再伪装成批量离线。

### 修复

- 合入上游 Issue #2 / PR #1 的命令固定参数与 `MessageChain` 契约修复，并扩展到全部相关命令。
- 合入 Issue #3：Komari 的 `0..1%` 低 CPU 不再被放大 100 倍。
- 同时支持 Komari 1.4 WebSocket 扁平 `cpu/ram/disk` 与 1.5 嵌套指标结构。
- 修复 `ws_connect` 超时参数类型、WebSocket `Origin` 校验、空在线列表与连接失败无法区分的问题。
- 只有重新观测到已超标指标回落后才推送负载恢复。
- 手动检查现在如实返回失败，不再始终显示成功。
- 修复 `/kg rt [节点]` 在合并实时状态时覆盖节点筛选参数的问题。

### 可靠性与安全

- 使用 `StarTools.get_data_dir()` 和原子替换保存状态。
- 运行时显式校验 `AstrBotConfig`，修正 Schema 控件与范围。
- 后台监控改为 `initialize()` 启动、`terminate()` 完整清理，每轮均有异常边界与退避。
- 管理、查询与高开销命令默认限制为 AstrBot 管理员；帮助命令对成员开放。
- `image_output` 默认关闭，避免在用户不知情时将节点数据交给 T2I 渲染服务。
- 回归套件更新为 46 项检查，并新增 GitHub Actions。

## 上游历史

v2.0.0 基于 `xiaowan138/astrbot_plugin_komari_watch` v1.4.0 及其完整 Git 历史开发。上游早期版本记录请查看原仓库。
